# 📊 Обзор проекта

Краткое резюме проекта для быстрого ознакомления.

---

## 🎯 Цель проекта

Демонстрация навыков разработки системы авторизации с гибкой матрицей доступа для тестового задания в аутстафф компанию.

---

## ✨ Ключевые особенности

### 🔐 Авторизация

- **RS256** - асимметричное шифрование (приватный + публичный ключ)
- **Мультиавторизация** - один пользователь на множестве устройств
- **Защита от подделки** - токены подписаны приватным ключом
- **Готовность к микросервисам** - публичный ключ можно распространять

**Почему RS256, а не HS256?**
- Безопасность: компрометация публичного ключа не критична
- Микросервисы: можно распространять публичный ключ для проверки токенов
- Масштабируемость: сервисы не могут создавать токены, только проверять

### 🗄️ База данных

- **3NF нормализация** - нет избыточности данных
- **ID-based подход** - INTEGER вместо VARCHAR для производительности (+2x скорость)
- **Супернормализованная структура** - roles → permissions → services/actions
- **Партиальные индексы** - уникальность email только для активных пользователей

### 🎯 Матрица доступа (RBAC)

- **Декоратор `@require_permission`** - простая проверка прав на уровне эндпоинтов
- **Управление через API** - полный CRUD без изменения кода
- **Производительность** - проверка прав < 0.1ms
- **Гибкость** - настройка политик доступа через админку

### 🐳 Docker

- **Docker Secrets** - безопасная передача JWT ключей без включения в образ
- **Multi-stage build** - оптимизированный размер образа
- **Healthcheck** - автоматическая проверка состояния сервисов

---

## 🏗️ Архитектура

```
User → JWT Middleware → @require_permission → Эндпоинт
         ↓                      ↓
    Проверка токена      Проверка прав в БД
    (RS256)              (< 0.1ms)
```

### Адресация эндпоинтов

```
/api/v1/
├── public/          # Без авторизации (register, login)
├── /users/          # Требует JWT (me, logout)
├── /analytics/      # Требует JWT + права (analytics:read)
├── /finance/        # Требует JWT + права (finance:read)
└── /admin/matrix/   # Требует роль admin (управление матрицей)
```

**Почему middleware?**
- Централизованная проверка токенов
- Автоматическое извлечение данных пользователя
- Разделение публичных и защищенных эндпоинтов через префиксы

---

## 📊 Производительность

| Операция | Время |
|----------|-------|
| Проверка прав (ID-based) | 0.08ms |
| Проверка прав (VARCHAR) | 0.15ms |
| Генерация JWT (RS256) | ~2ms |
| Проверка JWT (RS256) | ~1ms |
| Хеширование пароля (Argon2) | ~50ms |

**Выигрыш ID-based подхода:** ~2x по скорости, ~5x по размеру

---

## 🛠️ Технологии

- **Python 3.12** + **FastAPI** - веб-фреймворк
- **PostgreSQL 16** - реляционная БД (3NF)
- **Redis 7.2** - кэш и сессии
- **Docker & Docker Compose** - контейнеризация
- **Argon2** - хеширование паролей
- **JWT (RS256)** - авторизация
- **asyncpg** - асинхронный драйвер PostgreSQL

---

## 🔒 Безопасность

### Многоуровневая защита

1. **JWT токены (RS256)** - невозможно подделать без приватного ключа
2. **RBAC матрица** - гранулярный контроль доступа
3. **Argon2** - стойкое хеширование паролей
4. **HttpOnly cookies** - защита от XSS
5. **Rate limiting** - защита от брутфорса
6. **Docker Secrets** - ключи не в образе

### Почему редактирование cookies не прокатит?

```
Токен = Header.Payload.Signature

Signature = RS256(Header + Payload, private_key)

Если изменить Payload → Signature невалидна → 401 Unauthorized

Для создания новой подписи нужен приватный ключ (недоступен клиенту)
```

---

## 📁 Структура

```
project/
├── core/
│   ├── api/                    # API эндпоинты
│   │   ├── admin_matrix/       # Управление матрицей доступа
│   │   ├── services/           # Бизнес-сервисы
│   │   ├── users/              # Авторизация
│   │   └── middleware.py       # JWT middleware
│   ├── data/                   # Работа с БД
│   ├── utils/                  # Утилиты
│   │   ├── jwt_factory.py      # Генерация JWT
│   │   └── permissions_controller.py  # Декоратор @require_permission
│   └── main.py                 # Точка входа
├── secrets/                    # JWT ключи (НЕ в git, НЕ в образе)
├── docs/                       # Документация
├── docker-compose.yml          # Docker конфигурация
└── README.md                   # Главная документация
```

---

## 🚀 Быстрый старт

```bash
# 1. Генерация ключей (опционально, уже есть в репозитории)
openssl genrsa -out secrets/keys/private_jwt.pem 2048
openssl rsa -in secrets/keys/private_jwt.pem -outform PEM -pubout -out secrets/keys/public_jwt.pem

# 2. Запуск
docker-compose up --build -d

# 3. Проверка
curl http://127.0.0.1:8100/api/v1/public/healthcheck

# 4. Swagger UI
# http://127.0.0.1:8100/api/v1/public/docs
```

---

## 📚 Документация

- **[README.md](README.md)** - главная документация
- **[docs/AUTHORIZATION.md](docs/AUTHORIZATION.md)** - JWT, RS256, middleware
- **[docs/DATABASE.md](docs/DATABASE.md)** - структура БД, нормализация
- **[docs/ACCESS_MATRIX.md](docs/ACCESS_MATRIX.md)** - RBAC, декоратор
- **[docs/API_REFERENCE.md](docs/API_REFERENCE.md)** - все эндпоинты
- **[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)** - архитектура проекта

---

## 💡 Ключевые решения

### 1. RS256 вместо HS256

**Проблема:** HS256 требует общий ключ на всех сервисах  
**Решение:** RS256 - приватный ключ для подписи, публичный для проверки  
**Результат:** Безопасность + готовность к микросервисам

### 2. ID-based подход

**Проблема:** JOIN по VARCHAR медленный, опечатки в строках  
**Решение:** INTEGER ID + датаклассы для IDE autocomplete  
**Результат:** +2x скорость, типобезопасность

### 3. Middleware для JWT

**Проблема:** Дублирование кода проверки токенов в каждом эндпоинте  
**Решение:** Централизованная проверка в middleware  
**Результат:** DRY, безопасность, производительность

### 4. Docker Secrets

**Проблема:** Ключи не должны попасть в Docker образ  
**Решение:** Монтирование через Docker Secrets во время запуска  
**Результат:** Безопасность, можно публиковать образ

### 5. Нормализация (3NF)

**Проблема:** Избыточность данных, сложность изменений  
**Решение:** Нормализация до 3NF с ID-based подходом  
**Результат:** Нет дублирования, гибкость, производительность

---

## 🎓 Что демонстрирует проект

### Backend разработка

- ✅ FastAPI (async/await)
- ✅ PostgreSQL (сложные запросы, нормализация)
- ✅ Redis (кэширование, сессии)
- ✅ Pydantic (валидация данных)

### Безопасность

- ✅ JWT (RS256)
- ✅ Argon2 (хеширование паролей)
- ✅ RBAC (матрица доступа)
- ✅ Rate limiting
- ✅ Docker Secrets

### Архитектура

- ✅ Middleware pattern
- ✅ Decorator pattern
- ✅ Repository pattern
- ✅ Dependency Injection

### DevOps

- ✅ Docker & Docker Compose
- ✅ Multi-stage builds
- ✅ Healthchecks
- ✅ Secrets management

### Документация

- ✅ Подробная документация
- ✅ Swagger UI
- ✅ Примеры запросов
- ✅ Диаграммы

---
