# 🔐 JWT Authorization System with Access Matrix

> **⚠️ ВАЖНО:** В репозитории намеренно оставлены секреты (ключи, .env) для упрощения проверки проекта. В production они должны быть исключены из git!

Система авторизации на базе JWT (RS256) с гибкой матрицей доступа (RBAC) для управления правами пользователей.

---

## 🚀 Быстрый старт

### 1. Генерация JWT ключей (если нужны новые)

```bash
# Приватный ключ
openssl genrsa -out secrets/keys/private_jwt.pem 2048

# Публичный ключ
openssl rsa -in secrets/keys/private_jwt.pem -outform PEM -pubout -out secrets/keys/public_jwt.pem
```

### 2. Запуск через Docker (рекомендуется)

```bash
# Запустить все сервисы
docker-compose up --build -d

# Проверить статус
docker-compose ps

# Проверить healthcheck
curl http://127.0.0.1:8100/api/v1/public/healthcheck
```

### 3. Локальный запуск (для разработки)

```bash
# Активировать окружение
.venv\Scripts\activate

# Запустить приложение
uvicorn core.main:app
# Или
python -m core.main
```

### 4. Swagger UI

Интерактивная документация API:
- **Docker:** http://127.0.0.1:8100/api/v1/public/docs
- **Локально:** http://127.0.0.1:8000/api/v1/public/docs

---

## 📚 Документация

### Основные разделы

- **[Быстрый старт](docs/QUICK_START.md)** - запуск за 5 минут ⭐
- **[Обзор проекта](PROJECT_OVERVIEW.md)** - краткое резюме
- **[Авторизация](docs/AUTHORIZATION.md)** - JWT, RS256, middleware, мультиавторизация
- **[База данных](docs/DATABASE.md)** - структура, нормализация, матрица доступа
- **[Матрица доступа](docs/ACCESS_MATRIX.md)** - RBAC, декоратор, управление правами
- **[API Reference](docs/API_REFERENCE.md)** - все эндпоинты и примеры

### Дополнительно

- **[Быстрый старт](docs/QUICK_START.md)** - запуск за 5 минут
- **[Архитектура](docs/ARCHITECTURE.md)** - структура проекта и компоненты

---

## ✨ Ключевые особенности

### 🔐 Авторизация

- **RS256 (асимметричное шифрование)** - приватный ключ для подписи, публичный для проверки
- **Access Token + Refresh Token** - безопасное обновление сессий
- **Мультиавторизация** - один пользователь может быть авторизован на множестве устройств
- **Защита от подделки** - токены подписаны приватным ключом, изменение невозможно
- **Middleware** - централизованная проверка токенов для всех защищенных эндпоинтов
- **Готовность к микросервисам** - публичный ключ можно распространить между сервисами

### 🗄️ База данных

- **3NF нормализация** - оптимизированная структура без избыточности
- **ID-based подход** - использование INTEGER вместо VARCHAR для производительности
- **Матрица доступа** - гибкая система управления правами (roles → permissions → services/actions)
- **Soft-delete** - пользователи помечаются как неактивные, а не удаляются
- **Партиальные индексы** - уникальность email только для активных пользователей

### 🎯 Матрица доступа

- **Декоратор `@require_permission`** - простая проверка прав на уровне эндпоинтов
- **Управление через API** - полный CRUD для ролей, сервисов, действий, прав
- **Производительность** - проверка прав < 0.1ms благодаря ID-based подходу
- **Гибкость** - настройка политик доступа без изменения кода

### 🐳 Docker

- **Docker Secrets** - безопасная передача JWT ключей без включения в образ
- **Multi-stage build** - оптимизированный размер образа
- **Healthcheck** - автоматическая проверка состояния сервисов
- **Изоляция** - PostgreSQL, Redis, приложение в отдельных контейнерах

---

## 🏗️ Архитектура

### Структура проекта

```
project/
├── core/
│   ├── api/                    # API эндпоинты
│   │   ├── admin_matrix/       # Управление матрицей доступа
│   │   ├── services/           # Бизнес-сервисы (analytics, finance, etc.)
│   │   ├── users/              # Авторизация и пользователи
│   │   └── middleware.py       # JWT middleware
│   ├── config_dir/             # Конфигурация
│   ├── data/                   # Работа с БД
│   │   └── sql_queries/        # SQL запросы
│   ├── schemas/                # Pydantic схемы
│   └── utils/                  # Утилиты
│       ├── jwt_factory.py      # Генерация JWT токенов
│       └── permissions_controller.py  # Декоратор @require_permission
├── secrets/
│   ├── keys/                   # JWT ключи (RS256)
│   └── dumps/                  # SQL дампы для инициализации
├── docs/                       # Документация
├── docker-compose.yml          # Docker конфигурация
└── README.md                   # Этот файл
```

### Адресация эндпоинтов

```
/api/v1/
├── public/                     # Без авторизации
│   ├── /users/register         # Регистрация
│   ├── /users/login            # Вход
│   └── /healthcheck            # Проверка здоровья
│
├── /users/                     # Требует авторизацию (JWT)
│   ├── /me                     # Профиль пользователя
│   └── /logout                 # Выход
│
├── /analytics/                 # Требует авторизацию + права
├── /finance/                   # Требует авторизацию + права
├── /infrastructure/            # Требует авторизацию + права
│
└── /admin/                     # Требует роль admin
    └── matrix/                 # Управление матрицей доступа
        ├── /roles              # CRUD ролей
        ├── /services           # CRUD сервисов
        ├── /actions            # CRUD действий
        └── /permissions        # CRUD прав
```

**Почему middleware?**
- Централизованная проверка токенов
- Автоматическое извлечение данных пользователя из токена
- Разделение публичных и защищенных эндпоинтов через префиксы
- Нет дублирования кода проверки в каждом эндпоинте

---

## 🔑 Авторизация

### Почему RS256, а не HS256?

| Аспект | RS256 (асимметричное) | HS256 (симметричное) |
|--------|----------------------|---------------------|
| Ключи | Приватный + Публичный | Один общий ключ |
| Подпись | Только приватным ключом | Общим ключом |
| Проверка | Публичным ключом | Общим ключом |
| Микросервисы | ✅ Можно распространить публичный ключ | ❌ Нужно передавать секретный ключ |
| Безопасность | ✅ Компрометация публичного ключа не критична | ❌ Компрометация ключа = полный доступ |

**Для микросервисов:**
- Сервис авторизации подписывает токены приватным ключом
- Другие сервисы проверяют токены публичным ключом
- Публичный ключ можно безопасно распространять

### Мультиавторизация

Пользователь может быть авторизован на множестве устройств одновременно:

```
User: admin@example.com
├── Session 1: Chrome (Windows)    → access_token_1, refresh_token_1
├── Session 2: Firefox (Windows)   → access_token_2, refresh_token_2
└── Session 3: Mobile (Android)    → access_token_3, refresh_token_3
```

Каждая сессия хранится в БД с:
- `user_agent` - браузер/устройство
- `ip` - IP адрес
- `expires_at` - время истечения

### Защита от подделки токенов

**Почему редактирование cookies не прокатит:**

1. **Подпись токена** - токен подписан приватным ключом через RS256
2. **Проверка подписи** - при каждом запросе middleware проверяет подпись публичным ключом
3. **Изменение = невалидная подпись** - любое изменение payload делает подпись невалидной
4. **Приватный ключ недоступен** - злоумышленник не может создать валидную подпись

```
Токен = Header.Payload.Signature

Signature = RS256(Header + Payload, private_key)

Проверка:
  RS256_verify(Signature, Header + Payload, public_key)
  
Если Payload изменен → Signature невалидна → 401 Unauthorized
```

---

## 🗄️ База данных

### Нормализация

Нормализованная структура без избыточности:

```
users                   roles                   services
├── id                  ├── id                  ├── id
├── email               ├── name                ├── name
├── passw (Argon2)      └── description         └── description
├── role_id (FK)                │
└── is_active                   │               actions
                                │               ├── id
sessions                        │               ├── name
├── id                          │               └── description
├── user_id (FK)                │
├── refresh_token               │               permissions
├── user_agent                  │               ├── id
├── ip                          │               ├── service_id (FK)
└── expires_at                  │               └── action_id (FK)
                                │
                                ↓
                        role_permissions
                        ├── role_id (FK)
                        └── permission_id (FK)
```

### ID-based подход

Вместо хранения строк используются INTEGER ID:

```python
# Датаклассы для IDE autocomplete
@dataclass
class Services:
    analytics: int = 1
    finance: int = 2
    infrastructure: int = 3

@dataclass
class Actions:
    read: int = 1
    write: int = 2
    delete: int = 3

# Использование в декораторе
@require_permission(Services.analytics, Actions.read)
async def get_metrics(request: Request):
    ...
```

**Преимущества:**
- ✅ Производительность: JOIN по INTEGER быстрее чем по VARCHAR
- ✅ Типобезопасность: IDE подсказывает доступные значения
- ✅ Консистентность: невозможно опечататься в названии
- ✅ Гибкость: можно переименовать сервис в БД без изменения кода

---

## 🎯 Матрица доступа

### Декоратор `@require_permission`

Простая проверка прав на уровне эндпоинтов:

```python
from core.utils.permissions_controller import require_permission
from core.utils.anything import Services, Actions

@router.get('/analytics/metrics')
@require_permission(Services.analytics, Actions.read)
async def get_metrics(request: Request):
    # Если прав нет → 403 Forbidden
    # Если права есть → выполняется код
    return {"metrics": [...]}
```

**Как работает:**
1. Middleware извлекает `role` из JWT токена
2. Декоратор проверяет наличие права в БД (< 0.1ms)
3. Если права нет → 403 Forbidden
4. Если права есть → выполняется эндпоинт

### Управление через API

Полный CRUD для всех компонентов матрицы доступа:

```bash
# Создать новый сервис
POST /api/v1/admin/matrix/services
{
  "name": "reporting",
  "description": "Отчеты и аналитика"
}

# Создать новое действие
POST /api/v1/admin/matrix/actions
{
  "name": "export",
  "description": "Экспорт данных"
}

# Создать право (связь сервис + действие)
POST /api/v1/admin/matrix/permissions
{
  "service_id": 4,
  "action_id": 4
}

# Назначить право роли
POST /api/v1/admin/matrix/roles/{role_id}/permissions
{
  "permission_id": 10
}
```

**Гибкость:**
- Настройка политик доступа без изменения кода
- Динамическое добавление новых сервисов и действий
- Управление правами через админку

---

## 🐳 Docker и безопасность

### Docker Secrets

JWT ключи передаются через Docker Secrets, **не включаются в образ**:

```yaml
# docker-compose.yml
services:
  web_app:
    secrets:
      - private_key.pem
      - public_key.pem

secrets:
  private_key.pem:
    file: ./secrets/keys/private_jwt.pem
  public_key.pem:
    file: ./secrets/keys/public_jwt.pem
```

**Безопасность:**
- ✅ Ключи монтируются в `/run/secrets/` во время запуска
- ✅ Ключи НЕ копируются в образ (через `.dockerignore`)
- ✅ Образ можно безопасно публиковать в registry
- ✅ Работает локально и в Docker (автоопределение)


---

## 📊 Производительность

- **Проверка прав:** < 0.1ms (ID-based подход с индексами)
- **Генерация JWT:** ~2ms (RS256)
- **Проверка JWT:** ~1ms (RS256)
- **Регистрация:** ~50ms (Argon2 hashing)
- **Вход:** ~50ms (Argon2 verify)


---

## 🛠️ Технологии

- **Python 3.12** - язык программирования
- **FastAPI** - веб-фреймворк
- **PostgreSQL 16** - реляционная БД
- **Redis 7.2** - кэш и сессии
- **Docker & Docker Compose** - контейнеризация
- **Pydantic** - валидация данных
- **Argon2** - хеширование паролей
- **JWT (RS256)** - авторизация
- **asyncpg** - асинхронный драйвер PostgreSQL

---

## 📝 Требования

- Docker & Docker Compose
- Python 3.12+ (для локального запуска)
- OpenSSL (для генерации ключей)

---

## 🤝 Разработка

### Структура кода

- `core/api/` - API эндпоинты (роутеры)
- `core/data/` - работа с БД (SQL запросы)
- `core/schemas/` - Pydantic схемы
- `core/utils/` - утилиты (JWT, permissions, logger)
- `core/config_dir/` - конфигурация

### Добавление нового сервиса

1. Добавить сервис в БД через API или SQL
2. Обновить `core/utils/anything.py`:
   ```python
   @dataclass
   class Services:
       new_service: int = 5
   ```
3. Создать роутер в `core/api/services/new_service_api.py`
4. Использовать декоратор:
   ```python
   @require_permission(Services.new_service, Actions.read)
   ```

