# 🏗️ Архитектура

Обзор архитектуры проекта и ключевых компонентов.

---

## Структура проекта

```
project/
├── core/                       # Основной код приложения
│   ├── api/                    # API эндпоинты
│   │   ├── admin_matrix/       # Управление матрицей доступа
│   │   │   ├── roles_api.py
│   │   │   ├── services_api.py
│   │   │   ├── actions_api.py
│   │   │   └── permissions_api.py
│   │   ├── services/           # Бизнес-сервисы
│   │   │   ├── analytics_api.py
│   │   │   ├── finance_api.py
│   │   │   └── infrastructure_api.py
│   │   ├── users/              # Авторизация и пользователи
│   │   │   ├── users_api.py
│   │   │   └── rate_limiter.py
│   │   └── middleware.py       # JWT middleware
│   │
│   ├── config_dir/             # Конфигурация
│   │   ├── config.py           # Настройки (JWT ключи, БД, Redis)
│   │   └── env_modes.py        # Режимы окружения
│   │
│   ├── data/                   # Работа с БД
│   │   ├── postgres.py         # Подключение к PostgreSQL
│   │   ├── redis_storage.py    # Подключение к Redis
│   │   └── sql_queries/        # SQL запросы
│   │       ├── users_sql.py
│   │       ├── admin_sql.py
│   │       └── permissions_sql.py
│   │
│   ├── schemas/                # Pydantic схемы
│   │   ├── users_schema.py
│   │   ├── admin_schema.py
│   │   └── cookie_settings_schema.py
│   │
│   ├── utils/                  # Утилиты
│   │   ├── jwt_factory.py      # Генерация JWT токенов
│   │   ├── permissions_controller.py  # Декоратор @require_permission
│   │   ├── anything.py         # Датаклассы (Services, Actions, Roles)
│   │   ├── logger.py           # Логирование
│   │   └── lite_dependencies.py  # FastAPI dependencies
│   │
│   └── main.py                 # Точка входа
│
├── secrets/                    # Секреты (НЕ в git, НЕ в образе)
│   ├── keys/                   # JWT ключи
│   │   ├── private_jwt.pem
│   │   └── public_jwt.pem
│   └── dumps/                  # SQL дампы
│       └── 00_roles.sql
│
├── docs/                       # Документация
│   ├── AUTHORIZATION.md
│   ├── DATABASE.md
│   ├── ACCESS_MATRIX.md
│   ├── API_REFERENCE.md
│   └── ARCHITECTURE.md
│
├── docker-compose.yml          # Docker конфигурация
├── Dockerfile                  # Docker образ
├── .dockerignore               # Исключения для Docker
├── .gitignore                  # Исключения для git
├── .env                        # Переменные окружения
├── requirements.txt            # Python зависимости
└── README.md                   # Главная документация
```

---

## Компоненты

### 1. API Layer

**Роутеры FastAPI** - обработка HTTP запросов.

```
/api/v1/
├── public/          # Без авторизации
│   ├── /register
│   ├── /login
│   └── /refresh
│
├── /users/          # Требует JWT
│   ├── /me
│   └── /logout
│
├── /analytics/      # Требует JWT + права
├── /finance/        # Требует JWT + права
├── /infrastructure/ # Требует JWT + права
│
└── /admin/matrix/   # Требует роль admin
    ├── /roles
    ├── /services
    ├── /actions
    └── /permissions
```

### 2. Middleware

**JWT Middleware** - проверка токенов для всех защищенных эндпоинтов.

```python
Request → Middleware → Эндпоинт
           ↓
    1. Извлечь токен из cookies
    2. Проверить подпись (RS256)
    3. Добавить данные в request.state
    4. Пропустить дальше
```

### 3. Permissions Controller

**Декоратор @require_permission** - проверка прав доступа.

```python
@require_permission(Services.analytics, Actions.read)
async def get_metrics(request: Request):
    # Проверка прав в БД
    # Если прав нет → 403 Forbidden
    ...
```

### 4. Data Layer

**SQL запросы** - работа с PostgreSQL.

```
core/data/sql_queries/
├── users_sql.py        # Пользователи и сессии
├── admin_sql.py        # Админские операции
└── permissions_sql.py  # Матрица доступа
```

### 5. JWT Factory

**Генерация и проверка токенов** - RS256.

```python
# Генерация
access_token = jwt.encode(payload, private_key, algorithm="RS256")

# Проверка
payload = jwt.decode(token, public_key, algorithms=["RS256"])
```

---

## Поток запроса

### Публичный эндпоинт (регистрация)

```
1. POST /api/v1/public/users/register
   ↓
2. Middleware пропускает (префикс /public/)
   ↓
3. users_api.register()
   ↓
4. Хеширование пароля (Argon2)
   ↓
5. Сохранение в БД
   ↓
6. Ответ 201 Created
```

### Защищенный эндпоинт (профиль)

```
1. GET /api/v1/users/me
   Cookie: access_token=eyJhbGc...
   ↓
2. Middleware проверяет токен
   - Извлекает из cookies
   - Проверяет подпись (RS256)
   - Добавляет user_id, role в request.state
   ↓
3. users_api.get_me()
   - Получает user_id из request.state
   - Запрашивает данные из БД
   ↓
4. Ответ 200 OK
```

### Эндпоинт с проверкой прав

```
1. GET /api/v1/analytics/metrics
   Cookie: access_token=eyJhbGc...
   ↓
2. Middleware проверяет токен
   - request.state.role = "data_analyst"
   ↓
3. Декоратор @require_permission(Services.analytics, Actions.read)
   - Получает role из request.state
   - Проверяет в БД: есть ли право analytics:read
   - SQL: SELECT EXISTS (...)
   ↓
4. Если прав нет → 403 Forbidden
   Если права есть → analytics_api.get_metrics()
   ↓
5. Ответ 200 OK
```

---

## База данных

### Таблицы

```
users ─┬─→ sessions (1:N)
       └─→ roles (N:1)
              ↓
       role_permissions (N:M)
              ↓
         permissions
           ↓     ↓
      services  actions
```

### Нормализация

- **1NF** - нет повторяющихся групп
- **2NF** - все атрибуты зависят от первичного ключа
- **3NF** - нет транзитивных зависимостей

---

## Docker

### Сервисы

```yaml
services:
  pg_db:        # PostgreSQL 16
  redis:        # Redis 7.2
  web_app:      # FastAPI приложение
```

### Secrets

```yaml
secrets:
  private_key.pem:  # JWT приватный ключ
  public_key.pem:   # JWT публичный ключ
  .env:             # Переменные окружения
```

### Volumes

```yaml
volumes:
  pg_data:      # Данные PostgreSQL
  redis_data:   # Данные Redis
```

---

## Технологии

| Компонент | Технология | Версия |
|-----------|------------|--------|
| Язык | Python | 3.12 |
| Веб-фреймворк | FastAPI | latest |
| БД | PostgreSQL | 16 |
| Кэш | Redis | 7.2 |
| Контейнеризация | Docker | latest |
| Валидация | Pydantic | latest |
| Хеширование | Argon2 | latest |
| JWT | PyJWT | latest |
| БД драйвер | asyncpg | latest |

---

## Производительность

| Операция | Время |
|----------|-------|
| Проверка прав | < 0.1ms |
| Генерация JWT | ~2ms |
| Проверка JWT | ~1ms |
| Хеширование пароля | ~50ms |
| Проверка пароля | ~50ms |

---

## Безопасность

### Уровни защиты

1. **Авторизация** - JWT токены (RS256)
2. **Права доступа** - RBAC матрица
3. **Хеширование** - Argon2 для паролей
4. **Cookies** - HttpOnly, Secure, SameSite
5. **Rate limiting** - защита от брутфорса
6. **Docker Secrets** - безопасная передача ключей

---

## Масштабирование

### Горизонтальное

```
Load Balancer
    ↓
┌───────┬───────┬───────┐
│ App 1 │ App 2 │ App 3 │
└───────┴───────┴───────┘
    ↓       ↓       ↓
┌─────────────────────┐
│    PostgreSQL       │
│    (Primary)        │
└─────────────────────┘
```

### Вертикальное

- Увеличение ресурсов контейнеров
- Оптимизация SQL запросов
- Кэширование в Redis

---
